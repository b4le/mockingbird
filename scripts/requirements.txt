# Runtime dependencies for migrate_audio_to_gcs.py.
#
# Install with:
#   pip install --user -r scripts/requirements.txt
# Or via uv (preferred in this repo):
#   uv pip install -r scripts/requirements.txt
#
# Only required for LIVE runs (downloading from Drive). `--dry-run` and
# `--help` work on a vanilla Python 3.11+ install with no extra packages.
google-api-python-client>=2.140
google-auth>=2.32
google-auth-oauthlib>=1.2
